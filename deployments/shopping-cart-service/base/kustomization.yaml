apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: shopping-cart-service-template
    files:
      - application.yaml=application.yaml

generatorOptions:
  disableNameSuffixHash: true