apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products-service
  template:
    metadata:
      labels:
        app: products-service
    spec:
      containers:
        - name: products-service
          image: nicomesa/products-service:1.0.0
          envFrom:
            - configMapRef:
                name: products-service-config
            - secretRef:
                name: products-service-secrets
          volumeMounts:
            - name: config-volume
              mountPath: /config
              readOnly: true
          env:
            - name: SPRING_CONFIG_LOCATION
              value: /config/application.yaml
      volumes:
        - name: config-volume
          configMap:
            name: products-service-template